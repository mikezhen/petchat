name: Deploy to Firebase

on:
  push:
    branches:
      - main
      - pawcode-mvp   # remove this once pawcode-mvp is merged into main

# Cancel any in-progress deploy for the same branch
concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    name: Build & deploy
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # Authenticate to Google Cloud using the Firebase service account
      - uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PAWCODE_85F05 }}

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      # Deploy Firestore rules + indexes and Storage rules first
      - name: Deploy Firebase rules & indexes
        run: firebase deploy --only firestore,storage --project pawcode-85f05

      # Build the Next.js app with production env vars
      - name: Build Next.js app
        run: npm run build
        env:
          NEXT_PUBLIC_FIREBASE_API_KEY: ${{ secrets.NEXT_PUBLIC_FIREBASE_API_KEY_PAWCODE_85F05 }}
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: pawcode-85f05.firebaseapp.com
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: pawcode-85f05
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: pawcode-85f05.firebasestorage.app
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID_PAWCODE_85F05 }}
          NEXT_PUBLIC_FIREBASE_APP_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_APP_ID_PAWCODE_85F05 }}
          NEXT_PUBLIC_APP_URL: https://pawcode-85f05.web.app
          FIREBASE_PROJECT_ID: pawcode-85f05
          FIREBASE_CLIENT_EMAIL: ${{ secrets.FIREBASE_CLIENT_EMAIL_PAWCODE_85F05 }}
          FIREBASE_PRIVATE_KEY: ${{ secrets.FIREBASE_PRIVATE_KEY_PAWCODE_85F05 }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY_PAWCODE_85F05 }}

      # Deploy the Next.js app to Firebase App Hosting
      - name: Deploy to Firebase App Hosting
        run: firebase apphosting:backends:deploy --project pawcode-85f05 --backend pawcode-85f05
